package com.agent.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Zinc / Obsidian Foundation (Clean developer aesthetic)
val DarkBackground = Color(0xFF09090B)   // Zinc 950
val DarkSurface = Color(0xFF121215)      // Zinc 900
val DarkCard = Color(0xFF18181B)         // Zinc 850
val DarkCardElevated = Color(0xFF202024) // Zinc 800
val BorderSubtle = Color(0xFF27272A)     // Crisp 1dp border
val BorderLight = Color(0xFF3F3F46)      // Highlight border

// Refined Accents (Elegant, high-contrast, modern)
val AccentPrimary = Color(0xFF10B981)     // Refined Emerald
val AccentPrimaryHover = Color(0xFF059669)
val AccentSecondary = Color(0xFF0EA5E9)   // Clean Sky
val AccentIndigo = Color(0xFF6366F1)      // Electric Indigo

// Compatibility aliases
val GreenPrimary = AccentPrimary
val GreenSecondary = AccentPrimaryHover
val BlueAccent = AccentSecondary

// Typography Colors
val TextWhite = Color(0xFFFAFAFA)
val TextSecondary = Color(0xFFA1A1AA)
val TextMuted = Color(0xFF71717A)

// Status & Risk Alerts
val RedEmergency = Color(0xFFEF4444)     // Modern Red / Crimson
val YellowWarning = Color(0xFFF59E0B)    // Clean Amber
val StatusOnline = Color(0xFF22C55E)     // Subtle Green

// Terminal Palette (Matte graphite with readable soft syntax colors)
val TerminalBg = Color(0xFF0C0C0E)
val TerminalBar = Color(0xFF16161A)
val TerminalText = Color(0xFFE4E4E7)
val TerminalGreen = Color(0xFF4ADE80)
val TerminalDim = Color(0xFF71717A)
